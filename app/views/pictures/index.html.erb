<section class='container'>
    <% PreviewPicture.all.each do |preview_picture| %>

        <a class="fancybox" rel="group" href="#" ><%= image_tag preview_picture.image.url(:original), :class => 'grayscale' %></a>

    <% end %>
</section>

<% if user_signed_in? %>
<% end %>
  <%= link_to "Add Picture", new_preview_picture_path, :class => "add_preview_picture" %>